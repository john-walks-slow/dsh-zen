/**
 * Host half: no routes needed (pet spritesheet removed).
 * @module dsh-zen
 */

const name = "zen";
const inject: string[] = [];

function apply(_ctx: any) {
	// No host-side functionality
}

export { apply, inject, name };
